ogx := 800
ogy := 600
~+2::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((50/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~1::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((100/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~2::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((150/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~3::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((200/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~4::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((250/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~5::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((300/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~6::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((350/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~7::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((400/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~8::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((450/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~9::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((500/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~0::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((550/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}
~+1::{
	if WinActive("Plants vs. Zombies") {
	WinGetPos &a, &a, &x, &y
	ControlClick "x" String((625/ogx)*x) " y" String((60/ogy)*y), "A"
	}
}